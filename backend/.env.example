# 后端环境变量示例（提交到仓库）
# 复制为 backend/.env 后按需修改

# 服务端口
PORT=3001

# JWT 签名密钥（生产环境务必使用强随机值）
JWT_SECRET=change-me-in-production

# SQLite 数据库路径（可选，不设置则默认 backend/data/mindbalance.sqlite）
# DB_PATH=/absolute/path/to/mindbalance.sqlite

# 应用版本（可选）
APP_VERSION=0.1.0

# AI Provider Keys（可选）
# OPENAI_API_KEY=
# DEEPSEEK_API_KEY=
# QWEN_API_KEY=
